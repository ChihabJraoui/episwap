import { MaterialElevationAuthDirective } from './material-elevation-auth.directive';

describe('MaterialElevationAuthDirective', () => {
  it('should create an instance', () => {
    // const directive = new MaterialElevationAuthDirective();
    // expect(directive).toBeTruthy();
  });
});
